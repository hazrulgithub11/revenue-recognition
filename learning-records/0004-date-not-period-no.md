# No per-row period number

User chose dates only for Amortisation Period identity/order (`cf_date`). Contract `cf_rto_period` remains the count of rows to generate. No `cf_period_no` field on RTO Transactions.
